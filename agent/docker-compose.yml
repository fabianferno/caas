services:
  agent:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./skills:/app/skills
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "process.exit(0)"]
      interval: 30s
      timeout: 5s
      retries: 3
